apiVersion: v1
kind: Service
metadata:
  name: auto-repair-shop-execution-service
spec:
  type: NodePort
  selector:
    app: auto-repair-shop-execution
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082
      protocol: TCP
      name: http
