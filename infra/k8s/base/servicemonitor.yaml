apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: execution
  labels:
    release: prom
spec:
  selector:
    matchLabels:
      app: execution
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
